{
  "name": "test-go",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/alex-worker/TestGo.git",
  "author": "alex-worker <43813540+alex-worker@users.noreply.github.com>",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up --build",
    "test": "docker rm xeyes && ip=$(ifconfig en0 | grep inet | awk '$1==\"inet\" {print $2}') && xhost + $ip && docker run -d --name xeyes -e DISPLAY=$ip:0 -v /tmp/.X11-unix:/tmp/.X11-unix gns3/xeyes"
  }
}
